# runtime + build output (all generated state lives here)
# .output/ = preflight + per-slide demo state (shared/, slide-N/)
# dist/    = marp deck build (deck.md, index.html, deck.pdf, themes/)
.output/
dist/
node_modules/

# built CLI binary + backups
/forestrie
*.bak

# secrets / private keys, wherever they land
.env
.env.secret
*.pem

# stale artifacts from pre-.output/ runs (belt-and-suspenders)
/demo.env
/deployment.json
/genesis.cbor
/genesis-body.cbor
/root-grant.b64
/statement.json
/statement.cose
/receipt.cbor
/*.cbor
/*.cose
/*.b64
/*.sth
/*.log
/tmp/
